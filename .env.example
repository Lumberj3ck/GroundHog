NOTES_DIR=./notes
MASTER_PASSWORD=changeme
JWT_SECRET=replace_with_random_hex
OPENAI_API_KEY=your_groq_api_key

# Optional: Google OAuth flow (set when using --with-creds-oauth)
GOOGLE_CLIENT_ID=
GOOGLE_SECRET=
GOOGLE_REDIRECT_URL=http://localhost:8080/oauth/oauth2callback/

# Optional: Service account credentials file path passed via --with-creds-file
# GOOGLE_CREDENTIALS_FILE=./service-account.json
